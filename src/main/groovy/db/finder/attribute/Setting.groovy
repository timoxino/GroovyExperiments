package db.finder.attribute

class Setting
{
    def offset
    def step
    def amount

    Setting(offset, step, amount)
    {
        this.amount = amount
        this.step = step
        this.offset = offset
    }
}
